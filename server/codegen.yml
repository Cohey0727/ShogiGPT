schema: ./schema.graphql
generates:
  ./src/generated/graph/types.ts:
    plugins:
      - typescript
      - typescript-resolvers
    config:
      useIndexSignature: true
      useTypeImports: true
      maybeValue: T | null | undefined
